#include "types.h"
#include "stat.h"
#include "user.h"

int
main(int argc, char *argv[])
{
  if(argc < 1){
    printf(1, "No arguments!\n");
  }
  
  while(1)
  {
    printf(1, "stampa while\n");
  }

  exit();
  return 0;
}